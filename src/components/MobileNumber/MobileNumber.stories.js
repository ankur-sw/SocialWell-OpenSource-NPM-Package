import { MobileNumber } from "./MobileNumber";

export default {
  title: "Mobile Number",
  component: MobileNumber,
};

const Template = (args) => <MobileNumber />;

export const Default = Template.bind({});
